require "spec_helper"

describe SteamDonkey do
  it "has a version number" do
    expect(SteamDonkey::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
